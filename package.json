{
  "name": "lunar-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "Lich Am Viet Nam \u2014 engine chuyen doi + web app",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @lunar-calendar/sdk test",
    "dev": "pnpm --filter @lunar-calendar/sdk build && pnpm --filter @lunar-calendar/web dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}