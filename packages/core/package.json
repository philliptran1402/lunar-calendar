{
  "name": "@amlich/core",
  "version": "0.1.0",
  "description": "Engine lich am Viet Nam: chuyen doi duong<->am, can chi, tiet khi, gio hoang dao, ngay le. Zero dependency.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/*.test.js"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.2" }
}
